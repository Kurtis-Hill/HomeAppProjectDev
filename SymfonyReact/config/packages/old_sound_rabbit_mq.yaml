old_sound_rabbit_mq:
    connections:
        default:
            url: '%env(RABBITMQ_URL)%'
            lazy: false
            connection_timeout: 3
            read_write_timeout: 3

            # requires php-amqplib v2.4.1+ and PHP5.4+
#        keepalive: false

        # requires php-amqplib v2.4.1+
#        heartbeat: 0

        #requires php_sockets.dll
        use_socket: true # default false

    producers:
        upload_current_reading_sensor_data:
#            class: App\Producers\SensorProducers\SensorUpdateCurrentReadingProducer
#            service_alias: 'app.update.sensor.producer'
            connection: default
            exchange_options: { name: 'current-reading-upload-sensor-data', type: direct }
#                service_alias: home_app_service # no alias by default
    consumers:
        upload_current_reading_sensor_data:
            connection: default
            exchange_options: { name: 'current-reading-upload-sensor-data', type: direct }
            queue_options: { name: 'upload-current-reading-sensor-data' }
            callback: App\AMQP\UploadCurrentReadingSensorDataConsumer

#    producers:
#        # use 'old_sound_rabbit_mq.task_producer' service to send data.
#        task:
#            connection:       default
#            exchange_options: { name: 'task', type: direct }
#    consumers:
#        task:
#            connection:       default
#            exchange_options: { name: 'task', type: direct }
#            queue_options:    { name: 'task'}
#            callback:         App\Consumer\TaskConsumer
