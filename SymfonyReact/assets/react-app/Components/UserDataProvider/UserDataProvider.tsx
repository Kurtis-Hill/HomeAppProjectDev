import * as React from 'react';
import { useState, useEffect } from 'react';

export function UserDataProvider({ children }) {
    const [userGroups, setUserGroups] = useState([]);
    const [userRooms, setUserRooms] = userState([]);
}